block content
  h1= title

  - let items = results; 
  - let destination4 = `dispatch(\"/jet/create")` 
  - let o;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
    } 

  div(class="container-fluid") 
    button.btn( class="btn-warning", onclick= destination4) Add
  div(class="container-fluid") 
    div(class="row m-1") 
      each item in items  
        - let destination = `dispatch(\"/jet/update?id=${item._id}\")` 
        - let destination2 = `dispatch(\"/jet/delete?id=${item._id}\")`
        - let destination3 = `dispatch(\"/jet/detail?id=${item._id}\")`
        div(class="row-sm-2" class="m-1") Item #{item.jet_type}(#{item.jet_model}) is #{item.cost} 
          button.btn( class="btn-primary ml-1", onclick= destination) Update 
          button.btn( class="btn-secondary", onclick= destination2) Delete
          button.btn( class="btn-warning", onclick= destination3) View
script. 
  function dispatch(path){ 
    window.location.href=path 
  }